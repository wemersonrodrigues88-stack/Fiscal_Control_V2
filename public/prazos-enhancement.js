/* Compatibilidade: a tela de Prazos é renderizada pelo app.js.
   Este arquivo não deve substituir nem duplicar a tela principal. */
(function(){
  window.renderEnhancedPrazos = window.renderEnhancedPrazos || function(){};
})();