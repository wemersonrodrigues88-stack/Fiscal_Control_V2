/* Prazos — comportamento centralizado em app.js.
   Este arquivo permanece carregado por compatibilidade com versões anteriores,
   mas não altera os controles da tela. */
(function(){'use strict';})();