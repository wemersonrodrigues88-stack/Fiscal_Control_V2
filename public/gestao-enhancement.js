(()=>{
const SITUATIONS=['Ativo','Férias','Licença médica','Desligado','Pediu demissão'];
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
let teamCache=[];
const activeTeam=()=>teamCache.filter(u=>u.status==='active'&&u.profile!=='Desenvolvedor'&&u.profile!=='Gestão');
function isPage(p){return document.querySelector('#page-title')?.textContent.trim()===p}
async function team(){teamCache=(await api('/api/team-status')).data||[];return teamCache}
function install(){if(document.getElementById('gestao-final-style'))return;const s=document.createElement('style');s.id='gestao-final-style';s.textContent=`
.gestao-person{cursor:pointer;position:relative;text-align:left;width:100%}.gestao-person:hover{border-color:#9fb3cc;box-shadow:0 4px 14px rgba(15,23,42,.06)}
.gestao-person small{display:block;color:#718096;margin-top:4px}.gestao-situation{display:block;margin-top:4px;font-size:11px;font-weight:700;color:#536176}
.gestao-modal{position:fixed;inset:0;background:rgba(15,23,42,.42);display:flex;align-items:center;justify-content:center;padding:20px;z-index:9999}.gestao-dialog{background:#fff;border-radius:14px;padding:22px;max-width:520px;width:100%;max-height:92vh;overflow:auto;box-shadow:0 18px 50px rgba(15,23,42,.2)}.gestao-dialog h3{margin:0 0 5px}.gestao-dialog .muted{color:#718096;font-size:13px}.gestao-form{display:grid;gap:12px;margin-top:18px}.gestao-form .field{display:flex;flex-direction:column;gap:6px}.gestao-form label{font-size:12px;font-weight:700;color:#536176}.gestao-form input,.gestao-form select{width:100%;height:42px;border:1px solid #d9e0ea;border-radius:9px;padding:0 12px;background:#fff;font:inherit}.gestao-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:4px}.gestao-actions button{height:40px;border:1px solid #d9e0ea;border-radius:8px;padding:0 14px}.gestao-actions .primary{background:#17243a;color:#fff}.equipe-filters{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;margin:0 0 16px}.equipe-filters label{display:flex;flex-direction:column;gap:6px;font-size:12px;font-weight:700;color:#536176}.equipe-filters select{height:40px;border:1px solid #d9e0ea;border-radius:8px;padding:0 10px;background:#fff;font:inherit}.equipe-count{font-size:12px;color:#718096;margin-bottom:10px}.assistente-monitor{margin-top:16px}.assistente-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.assistente-card{border:1px solid #e2e7ef;border-radius:10px;padding:12px;background:#fff;cursor:pointer;text-align:left}.assistente-card:hover{border-color:#9fb3cc}.assistente-card strong{display:block;font-size:13px}.assistente-card small{display:block;color:#718096;margin-top:3px}.assistente-empty{padding:18px;text-align:center;color:#718096;border:1px solid #e2e7ef;border-radius:10px}@media(max-width:760px){.equipe-filters,.assistente-grid{grid-template-columns:1fr}}
`;document.head.appendChild(s)}
async function openEdit(u){const all=(await api('/api/team')).data||[];const coords=all.filter(x=>x.profile==='Coordenador'&&x.status==='active');const managers=all.filter(x=>['Gestão','Desenvolvedor'].includes(x.profile)&&x.status==='active');const m=document.createElement('div');m.className='gestao-modal';m.innerHTML=`<div class="gestao-dialog"><h3>Cadastro do colaborador</h3><div class="muted">Cadastro de ${esc(u.name)}</div><form class="gestao-form" id="ge-form"><div class="field"><label>Nome</label><input name="name" value="${esc(u.name)}" required></div><div class="field"><label>Usuário</label><input name="username" value="${esc(u.username)}" required></div><div class="field"><label>Perfil</label><select name="profile"><option ${u.profile==='Analista'?'selected':''}>Analista</option><option ${u.profile==='Assistente'?'selected':''}>Assistente</option><option ${u.profile==='Coordenador'?'selected':''}>Coordenador</option></select></div><div class="field"><label>Senioridade</label><select name="seniority"><option value="junior" ${u.seniority==='junior'?'selected':''}>Júnior</option><option value="pleno" ${u.seniority==='pleno'?'selected':''}>Pleno</option><option value="senior" ${u.seniority==='senior'?'selected':''}>Sênior</option></select></div><div class="field"><label>Coordenador</label><select name="coordinator_user_id"><option value="">Selecione</option>${coords.map(x=>`<option value="${x.id}" ${String(x.id)===String(u.coordinator_user_id)?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="field"><label>Gerente</label><select name="manager_user_id"><option value="">Selecione</option>${managers.map(x=>`<option value="${x.id}" ${String(x.id)===String(u.manager_user_id)?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="field"><label>Situação do colaborador</label><select name="situation">${SITUATIONS.map(x=>`<option ${x===(u.situation||'Ativo')?'selected':''}>${x}</option>`).join('')}</select></div><div class="gestao-actions"><button type="button" id="ge-cancel">Cancelar</button><button class="primary" type="submit">Salvar cadastro</button></div></form></div>`;document.body.appendChild(m);const f=m.querySelector('#ge-form');const close=()=>m.remove();m.querySelector('#ge-cancel').onclick=close;m.onclick=e=>{if(e.target===m)close()};f.onsubmit=async e=>{e.preventDefault();const b=f.querySelector('button[type=submit]');b.disabled=true;b.textContent='Salvando...';const fd=new FormData(f);try{await api('/api/team-status',{method:'PUT',body:JSON.stringify({user_id:u.id,name:fd.get('name'),username:fd.get('username'),profile:fd.get('profile'),seniority:fd.get('seniority')||null,coordinator_user_id:fd.get('coordinator_user_id')?Number(fd.get('coordinator_user_id')):null,manager_user_id:fd.get('manager_user_id')?Number(fd.get('manager_user_id')):null,situation:fd.get('situation')})});close();await renderManagement()}catch(err){alert(err.message);b.disabled=false;b.textContent='Salvar cadastro'}}}
async function renderEquipe(c){const rows=(await api('/api/team')).data||[];c.innerHTML=`<div class="section-title"><div><h2>Equipe</h2><p class="muted">Todos os colaboradores autorizados para o seu nível de acesso.</p></div></div>`;const senior=[...new Set(rows.map(x=>x.seniority).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt-BR'));const coords=[...new Set(rows.map(x=>x.coordinator_name).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt-BR'));c.insertAdjacentHTML('beforeend',`<div class="equipe-filters"><label>Senioridade<select id="eq-senioridade"><option value="">Todas</option>${senior.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join('')}</select></label><label>Coordenador<select id="eq-coordenador"><option value="">Todos</option>${coords.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join('')}</select></label></div><div class="equipe-count" id="eq-count"></div><div id="eq-table"></div>`);const s=c.querySelector('#eq-senioridade'),co=c.querySelector('#eq-coordenador'),count=c.querySelector('#eq-count'),root=c.querySelector('#eq-table');const draw=()=>{const filtered=rows.filter(r=>(!s.value||r.seniority===s.value)&&(!co.value||r.coordinator_name===co.value));count.textContent=`${filtered.length} colaborador(es)`;root.innerHTML=filtered.length?`<div class="table-wrap"><table><thead><tr><th>Colaborador</th><th>Perfil</th><th>Senioridade</th><th>Coordenador</th><th>Gerente</th><th>Carteiras</th></tr></thead><tbody>${filtered.map(r=>`<tr><td>${esc(r.name??'—')}</td><td>${esc(r.profile??'—')}</td><td>${esc(r.seniority??'—')}</td><td>${esc(r.coordinator_name??'—')}</td><td>${esc(r.manager_name??'—')}</td><td>${esc(r.portfolio_count??0)}</td></tr>`).join('')}</tbody></table></div>`:'<div class="card empty">Nenhum colaborador encontrado.</div>'};[s,co].forEach(x=>x.onchange=draw);draw()}
async function renderManagement(){if(!isPage('Gestão'))return;install();const c=document.querySelector('#content');if(!c)return;await team();const active=activeTeam();const assistants=teamCache.filter(x=>x.status==='active'&&x.profile==='Assistente');const analysts=teamCache.filter(x=>x.status==='active'&&x.profile==='Analista');const coords=teamCache.filter(x=>x.status==='active'&&x.profile==='Coordenador');const managerUsers=active;const storeList=()=>state.data?.stores||[];const assignedByName=n=>storeList().filter(s=>s.analyst===n);const progress=n=>{const stores=assignedByName(n),items=state.data?.executions||[];const total=stores.length*TAX.length;if(!total)return 0;const done=items.filter(x=>stores.some(s=>String(s.id)===String(x.store_id))&&x.status==='Finalizado').length;return Math.round(done/total*100)};c.innerHTML=`<div class="section-title"><div><h2>Gestão da equipe</h2><p class="muted">Acompanhamento individual e administração dos acessos.</p></div>${['Gestão','Desenvolvedor'].includes(state.user.profile)?'<button class="primary action-button" id="new-collaborator">+ Novo colaborador</button>':''}</div><div class="grid kpis"><div class="card kpi"><div class="label">Analistas ativos</div><div class="value">${analysts.length}</div></div><div class="card kpi"><div class="label">Assistentes ativos</div><div class="value">${assistants.length}</div></div><div class="card kpi"><div class="label">Lojas ativas</div><div class="value">${storeList().length}</div></div><div class="card kpi"><div class="label">Conclusão geral</div><div class="value">${(()=>{const total=storeList().length*TAX.length,done=(state.data?.executions||[]).filter(x=>x.status==='Finalizado').length;return total?Math.round(done/total*100):0})()}%</div></div><div class="card kpi"><div class="label">Carteiras</div><div class="value">${analysts.filter(a=>assignedByName(a.name).length).length}</div></div></div><div class="card section"><div class="title"><h3>Acompanhamento individual</h3><span class="badge blue">${active.length} colaboradores ativos</span></div><div class="grid three">${active.map(u=>`<button type="button" class="card gestao-person" data-person="${u.id}"><b>${esc(u.name)}</b><small>${esc(u.profile)}${u.seniority?` · ${esc(u.seniority)}`:''} · ${assignedByName(u.name).length} lojas · ${progress(u.name)}%</small><span class="gestao-situation">Situação: ${esc(u.situation||'Ativo')}</span></button>`).join('')}</div></div><div class="card assistente-monitor"><div class="title"><h3>Acompanhamento individual — Assistentes ativos</h3><span class="badge blue">${assistants.length}</span></div>${assistants.length?`<div class="assistente-grid">${assistants.map(u=>`<button type="button" class="assistente-card" data-person="${u.id}"><strong>${esc(u.name)}</strong><small>${esc(u.seniority||'—')} · ${assignedByName(u.name).length} lojas · ${progress(u.name)}% · Ativo</small></button>`).join('')}</div>`:'<div class="assistente-empty">Nenhum assistente ativo cadastrado.</div>'}</div>`;c.querySelectorAll('[data-person]').forEach(b=>b.onclick=e=>{e.preventDefault();e.stopPropagation();const u=teamCache.find(x=>String(x.id)===b.dataset.person);if(u)openEdit(u)});const create=c.querySelector('#new-collaborator');if(create&&typeof window.openCreate==='function')create.onclick=()=>window.openCreate()}
window.equipe=renderEquipe;window.managementPage=renderManagement;let last='';const observe=async()=>{const p=document.querySelector('#page-title')?.textContent.trim()||'';if(p!==last){last=p;if(p==='Equipe'){setTimeout(()=>renderEquipe(document.querySelector('#content')),80)}if(p==='Gestão'){setTimeout(renderManagement,100)}else document.getElementById('assistentes-floating')?.remove()}};new MutationObserver(observe).observe(document.documentElement,{childList:true,subtree:true});setTimeout(observe,250);
})();